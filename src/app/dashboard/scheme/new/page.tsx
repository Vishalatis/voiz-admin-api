import { SchemeCreateView } from 'src/sections/scheme/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Scheme',
};

export default function PostCreatePage() {
  return <SchemeCreateView />;
}
