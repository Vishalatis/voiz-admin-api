import { SchemeListView } from 'src/sections/scheme/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Scheme List',
};

export default function PostListPage() {
  return <SchemeListView />;
}
