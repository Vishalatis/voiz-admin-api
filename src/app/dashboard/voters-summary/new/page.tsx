// ----------------------------------------------------------------------

import { VotersSummaryCreateView } from 'src/sections/voters-summary/view';

export const metadata = {
  title: 'Dashboard: Create a new Voters Summary',
};

export default function JobCreatePage() {
  return <VotersSummaryCreateView />;
}
